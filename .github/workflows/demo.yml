name: Create issue on commit

on: [ push ]

jobs:
  create_issue:
    runs-on: ubuntu-latest
    permissions:
      issues: write
    steps:
      - name: Create issue using REST API
        run: |
         curl -X GET "https://services.odata.org/v4/northwind/northwind.svc/Customers"
